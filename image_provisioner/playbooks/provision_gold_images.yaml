---
- name: provision gold images
  user: root
  hosts: all

  roles:
    - os-kickstart
    - clone-repos
    - aos-ansible
    - repo-install
    - collectd-install
    - pbench-config
    - docker-config
    - svt-setup
    - openshift-package-install
    - seal-image
