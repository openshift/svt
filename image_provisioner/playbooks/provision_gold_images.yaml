---
- name: provision gold images
  user: root
  hosts: all

  roles:
    - repo-install
    - os-kickstart
    - collectd-install
    - pbench-config
    - clone-repos
    - docker-config
    - aos-ansible
    - svt-setup
    - openshift-package-install
    - seal-image

