---
- name: provision gold images
  user: root
  hosts: all

  roles:
    - os-kickstart
    - clone-repos
    - aos-ansible
    - repo-install
    - collectd-install
    - clone-repos
    - pbench-config
    - docker-config
    - aos-ansible
    - svt-setup
    - openshift-package-install
    - seal-image
