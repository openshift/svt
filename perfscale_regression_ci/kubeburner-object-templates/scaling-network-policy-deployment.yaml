apiVersion: apps/v1
kind: Deployment
metadata:
  name: np-issue-test
spec:
  replicas: {{.pod_replicas}}
  selector:
    matchLabels:
      app: np-issue-test
  template:
    metadata:
      labels:
        app: np-issue-test
    spec:
      containers:
      - image: gcr.io/google-containers/pause-amd64:3.0
        imagePullPolicy: IfNotPresent
        name: pause